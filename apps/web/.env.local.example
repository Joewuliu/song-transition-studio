# Base URL the frontend calls for the backend API. Defaults to the local
# FastAPI dev server. For a deployed backend, point this at its real URL,
# e.g. NEXT_PUBLIC_API_URL=https://your-api.example.com
NEXT_PUBLIC_API_URL=http://localhost:8000
